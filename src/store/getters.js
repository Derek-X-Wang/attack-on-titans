const getters = {
  interviewState(state) {
    return state.interviewState;
  },
  report(state) {
    return state.report;
  },
  records(state) {
    return state.records;
  },
};

export default getters;
